global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # OTel collector exposes metrics from all telescope pipeline services
  - job_name: otel-collector
    static_configs:
      - targets: ['otel-collector:8889']
        labels:
          namespace: telescope-obs
